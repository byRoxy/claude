<!DOCTYPE html>
<html lang="tr" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yönetim Paneli</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background-color: #09090b; color: white; }</style>
</head>
<body class="p-4 md:p-10 max-w-5xl mx-auto">
    
    <div class="flex justify-between items-center mb-10">
        <div>
            <h1 class="text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-purple-400">Yönetim Paneli</h1>
            <p class="text-gray-400">Kullanıcı erişimlerini yönet.</p>
        </div>
        <a href="/logout" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg text-sm transition">Çıkış Yap</a>
    </div>

    <div class="bg-[#18181b] border border-white/10 rounded-2xl p-6 mb-8 shadow-xl">
        <h2 class="text-xl font-bold mb-4 flex items-center gap-2"><span class="text-green-400">+</span> Yeni Anahtar Oluştur</h2>
        <form action="/admin/create_key" method="POST" class="flex gap-4">
            <input type="text" name="note" placeholder="Kullanıcı Adı / Not (Örn: Ahmet)" required
                class="flex-1 bg-black/30 border border-white/10 rounded-xl p-3 text-white focus:border-indigo-500 outline-none">
            <button type="submit" class="bg-indigo-600 hover:bg-indigo-500 px-6 py-3 rounded-xl font-bold transition shadow-lg shadow-indigo-900/20">
                Oluştur
            </button>
        </form>
    </div>

    <div class="overflow-hidden rounded-2xl border border-white/10 bg-[#18181b]">
        <table class="w-full text-left text-gray-300">
            <thead class="bg-white/5 text-gray-400 uppercase text-xs">
                <tr>
                    <th class="p-4">Not</th>
                    <th class="p-4">Erişim Anahtarı</th>
                    <th class="p-4">Sohbet Sayısı</th>
                    <th class="p-4">Oluşturulma</th>
                    <th class="p-4 text-right">İşlem</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-white/5">
                {% for user in users %}
                <tr class="hover:bg-white/5 transition">
                    <td class="p-4 font-bold text-white">{{ user[2] }}</td>
                    <td class="p-4 font-mono text-indigo-400 bg-black/20 rounded select-all">{{ user[1] }}</td>
                    <td class="p-4"><span class="bg-white/10 px-2 py-1 rounded text-xs">{{ user[4] }} sohbet</span></td>
                    <td class="p-4 text-xs text-gray-500">{{ user[3] }}</td>
                    <td class="p-4 text-right">
                        <form action="/admin/delete_key/{{ user[0] }}" method="POST" onsubmit="return confirm('Bu kullanıcıyı ve tüm sohbetlerini silmek istediğine emin misin?');">
                            <button type="submit" class="text-red-400 hover:text-red-300 hover:bg-red-500/10 p-2 rounded transition">
                                Sil 🗑️
                            </button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        
        {% if not users %}
        <div class="p-8 text-center text-gray-500 italic">Henüz hiç kullanıcı yok.</div>
        {% endif %}
    </div>

</body>
</html>
