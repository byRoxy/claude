<!DOCTYPE html>
<html lang="tr" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistem Girişi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background-color: #09090b; color: white; }</style>
</head>
<body class="flex items-center justify-center h-screen">
    <div class="w-full max-w-md p-8 bg-[#18181b] rounded-2xl border border-white/10 shadow-2xl">
        <div class="text-center mb-8">
            <div class="w-16 h-16 bg-indigo-600/20 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-4 border border-indigo-500/30">🔒</div>
            <h1 class="text-2xl font-bold">Erişim Paneli</h1>
            <p class="text-gray-400 text-sm mt-2">Devam etmek için kimlik doğrulaması yapın.</p>
        </div>

        <form method="POST" action="/login" class="space-y-4">
            <div>
                <input type="password" name="password" placeholder="Erişim Şifresi" required 
                    class="w-full bg-[#09090b] border border-white/10 rounded-xl p-4 text-white focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 outline-none transition">
            </div>
            
            {% if error %}
            <div class="text-red-400 text-sm text-center bg-red-500/10 p-2 rounded-lg border border-red-500/20">
                ⚠️ {{ error }}
            </div>
            {% endif %}

            <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-500 text-white font-bold py-4 rounded-xl transition shadow-lg shadow-indigo-900/20">
                Sisteme Giriş Yap
            </button>
        </form>
    </div>
</body>
</html>
